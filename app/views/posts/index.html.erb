<h3>Hello, World!</h3>

<% @posts.each do |post| %>
  <div>
    <h3><%= post.name %></h3>
    <hr>
    <p class="text"><%= post.body %><p>
    <%= link_to 'View Post', post %>
  </div>
  <hr>
<% end %>